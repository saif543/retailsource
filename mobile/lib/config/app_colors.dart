// Central place for all colors. Change one value here → updates everywhere.

import 'package:flutter/material.dart';

class AppColors {
  // Primary brand blue (used in headers, buttons, active icons)
  static const Color primary = Color(0xFF1E5FBD);
  static const Color primaryBlue = Color(0xFF1E5FBD);

  // Darker blue for header gradients
  static const Color primaryDark = Color(0xFF1549A0);

  // Green for supplier/stockholder actions
  static const Color supplierGreen = Color(0xFF2E7D32);
  static const Color primaryGreen = Color(0xFF2E7D32);

  // Orange badge color
  static const Color badgeOrange = Color(0xFFE65100);
  static const Color accentOrange = Color(0xFFE65100);

  // Error red
  static const Color errorRed = Color(0xFFC62828);

  // Light grey page background
  static const Color background = Color(0xFFF5F6FA);
  static const Color bgWhite = Color(0xFFFFFFFF);

  // White card background
  static const Color cardWhite = Color(0xFFFFFFFF);
  static const Color cardGrey = Color(0xFFF5F5F5);

  // Input field border
  static const Color inputBorder = Color(0xFFDDE1E7);

  // Body text
  static const Color textDark = Color(0xFF1A1A2E);

  // Subtitle / hint text
  static const Color textGrey = Color(0xFF6B7280);

  // Success green badge
  static const Color statusGreen = Color(0xFF4CAF50);

  // Open/pending badge blue
  static const Color statusBlue = Color(0xFF2196F3);
}
